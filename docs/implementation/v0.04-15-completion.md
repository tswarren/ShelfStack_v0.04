# v0.04-15 Products Overview Refactor — Completion

## Status

**Complete** — lightweight Overview tab per [spec](../v0.04/v0.04-15-products-overview-refactor/spec.md).

---

## Deliverables

### Presenters and services

* `Items::SubdepartmentDisplayResolver` — display-only subdepartment chain (variant → product default → store category default)
* `ItemPresenter` — `overview_eyebrow_label`, `overview_header_badges`, `product_facts_for_overview`, `description_summary_label`, extended `subject_groups` (Audiences, Access Restrictions)
* `ItemOverviewPresenter` — `summary_strip`, `availability_rows`

### Views

* `_overview_hero.html.erb` — rendered before tabs on Overview (`show.html.erb`)
* `_overview_summary_strip.html.erb`, `_overview_facts.html.erb`, `_overview_variant_table.html.erb`
* Refactored `_overview.html.erb` — strip, hidden `#warnings`, two-column body, variant ops drawer
* `_overview_subjects.html.erb` — More/Less overflow per rail section

### Controller

* `load_overview_presenter` — Overview tab only
* `load_operational_warnings` — Operations tab only

### CSS

* Additive rules in `shelfstack.domain.items.css` — summary strip, facts table, availability actions, subject overflow, description read-less

### Contracts

* Revised [phase-9-item-drill-down-contract.md](../handoff/phase-9-item-drill-down-contract.md)
* Integration tests: `items_item_overview_contract_test`, `items_item_ux_contract_test`, `items_items_controller_test`, new `items_overview_actions_test`
* Service/presenter tests: `subdepartment_display_resolver_test`, `item_overview_presenter_test`

---

## Out of scope (unchanged)

* Operations, Item setup, Activity tabs
* Items index
* Variant ops drawer body and demand form logic

---

## Verification

```bash
./dev/rails-docker bin/rails test test/integration/items_item_overview_contract_test.rb \
  test/integration/items_item_ux_contract_test.rb \
  test/integration/items_overview_actions_test.rb \
  test/integration/items_items_controller_test.rb \
  test/services/items/subdepartment_display_resolver_test.rb \
  test/presenters/items/item_overview_presenter_test.rb
./dev/rails-docker bin/rails test
```

Manual: Overview with catalog-linked book — hero before tabs, summary strip, variant table with Details + Request (with `demand.access`), description and rail More/Less.
