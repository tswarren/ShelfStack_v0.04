# v0.04-11 Documentation and Schema Cleanup — Completion Note

## Status

**Complete** — merged to `main` on 2026-07-03 via [PR #16](https://github.com/tswarren/ShelfStack_v0.04/pull/16).

---

## Deliverables

| Slice | Status | Notes |
| ----- | ------ | ----- |
| 0 Baseline | Done | v00410 G2 PASS before branch |
| A Audit | Done | Stale hits classified in spec + data-model audit log |
| B Canonical docs | Done | `domain-model.md`, `overview.md`, `glossary.md`, `AGENTS.md`, `README.md`, `docs/README.md` |
| C Schema reference | Done | Curated v0.04 sections; retired ordering; retained-temporary catalog |
| D Verifier | Done | `Shelfstack::V00411Verify` + rake + tests; stale nav + module pattern checks |
| E Catalog audit | Done | retain-temporary documented; no drop migration |
| G Code cleanup | Done | Demand-native drawer lists; hub struct rename; legacy stubs for customer requests only |
| F Closure | Done | This note; milestone status in `docs/v0.04/README.md` |

---

## Verification (2026-07-02)

```text
1696 runs, 5902 assertions, 0 failures, 0 errors, 1 skip

v0046–v0049: PASS (STRICT)
v00410 G2: PASS (STRICT)
v00411: PASS (STRICT)
```

Commands:

```bash
./dev/rails-docker bin/rails db:migrate
./dev/rails-docker bin/rails db:seed
./dev/rails-docker bin/rails test

STRICT=1 ./dev/rails-docker env STRICT=1 bin/rails shelfstack:v0046:verify_demand_foundation
STRICT=1 ./dev/rails-docker env STRICT=1 bin/rails shelfstack:v0047:verify_allocations
STRICT=1 ./dev/rails-docker env STRICT=1 bin/rails shelfstack:v0048:verify_sourcing
STRICT=1 ./dev/rails-docker env STRICT=1 bin/rails shelfstack:v0049:verify_po_receiving
./dev/rails-docker env V00410_PHASE=g2 STRICT=1 bin/rails shelfstack:v00410:verify_legacy_ordering_retired
STRICT=1 ./dev/rails-docker env STRICT=1 bin/rails shelfstack:v00411:verify_documentation_schema_cleanup
```

---

## Resolved decisions (2026-07-02)

* `catalog_items` — **retain-temporary**
* Redirect aliases — **keep**
* `from_tbo` — **keep deprecated**
* Schema reference — **curated**
* Phase specs — **banner only**
* Verifier scope — canonical guidance docs + `app/` + `routes.rb`

---

## Known compatibility shims (documented, non-blocking)

* `PurchaseOrderDocumentHub#purchase_requests` returns `[]` — legacy PO document trail field; rename deferred.
* `ItemOperationsTabPresenter` legacy method aliases (`open_purchase_request_lines`, etc.) delegate to demand queries.
* Variant drawer **Legacy activity** section only renders when v0.03 customer-request lines exist (always empty post v0.04-10).

---

## Deferred

* Phase 10-E consistency sweep (after v0.04-11 merge)
* v0.04-12+ catalog/import cleanup (`catalog_items` drop)
* `from_tbo` param rename to demand-native names
* Historical phase spec banners (optional)
* `PurchaseOrderDocumentHub` `:purchase_requests` field rename
* Migration squashing
