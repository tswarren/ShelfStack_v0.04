# v0.04-12 Demand Ordering UX — Completion Note

## Status

**Complete** on branch `v0.04.12-demand-sourcing-ordering-ux-completion` (2026-07-03).

---

## Deliverables

| Slice | Status | Notes |
| ----- | ------ | ----- |
| 0 Docs | Complete | Spec bundle, roadmap, v0.04-11 complete markers |
| B Next-action | Complete | `Demand::DemandLineWorkflowPresenter`, next-action/record summary/timeline partials; PO demand links fixed |
| A Capture | Complete | Standalone form with variant/customer lookups, prefill, supply preview, confirmation messages |
| C Allocation | Complete | Allocation workbench with inbound PO line picker (hidden IDs); allocate-all override gate |
| D Sourcing | Complete | Vendor cards, response wizard PO line select, "Mark as submitted to vendor" |
| E PO bridge | Complete | `DemandCoveragePlanner`, `BuildPurchaseOrderFromDemand`, `AddDemandToPurchaseOrder`; draft = planned only |
| F Receiving | Complete | Receipt post confirmation (customer-ready vs stock); demand-native receipt links |
| G POS | Complete | Pickup deep link (`?pickup=`), auto-open panel + search, demand number prefill |
| H Verifier | Complete | Slice-aware `V00412Verify` + rake task + tests |

---

## Cross-cutting rules enforced

- **Planned vs committed:** draft PO coverage labeled "Planned on order"; active inbound allocations only when `InboundAvailability` eligible (not on draft create).
- **No raw IDs:** normal staff flows use lookups, vendor cards, and inbound PO row actions.
- **Supply language:** `DemandLines::SupplySummary` four staff states shared across detail, capture preview, and workbench.

---

## Key files

| Area | Path |
| ---- | ---- |
| Workflow presenter | `app/presenters/demand/demand_line_workflow_presenter.rb` |
| Supply summary | `app/services/demand_lines/supply_summary.rb` |
| PO bridge | `app/services/purchasing/demand_coverage_planner.rb`, `build_purchase_order_from_demand.rb`, `add_demand_to_purchase_order.rb` |
| Inbound picker | `app/services/demand_allocations/eligible_inbound_lines.rb` |
| Verifier | `lib/shelfstack/v00412_verify.rb` |

---

## Verification

```text
./dev/rails-docker bin/rails test test/presenters/demand/ test/lib/shelfstack/v00412_verify_test.rb \
  test/integration/demand_allocations_controller_test.rb \
  test/integration/pos_demand_pickup_integration_test.rb \
  test/integration/items_customer_demand_drawer_integration_test.rb \
  test/services/demand_allocations/ test/services/demand_lines/ \
  test/services/pos/demand_pickup_lookup_test.rb test/services/pos/add_demand_allocation_line_test.rb \
  test/services/purchasing/receipt_line_demand_test.rb
→ 76 runs, 190 assertions, 0 failures

./dev/rails-docker bin/rails test
→ 1703 runs, 5910 assertions, 0 failures, 0 errors, 1 skip

STRICT=1 bin/rails shelfstack:v0046:verify_demand_foundation   → PASS
STRICT=1 bin/rails shelfstack:v0047:verify_allocations       → PASS
STRICT=1 bin/rails shelfstack:v0048:verify_sourcing          → PASS
STRICT=1 bin/rails shelfstack:v0049:verify_po_receiving       → PASS
V00410_PHASE=g2 STRICT=1 bin/rails shelfstack:v00410:verify_legacy_ordering_retired → PASS
STRICT=1 bin/rails shelfstack:v00411:verify_documentation_schema_cleanup → PASS
V00412_SLICE=final STRICT=1 bin/rails shelfstack:v00412:verify_demand_ordering_ux → PASS (18 checks)
```

---

## Known gaps / follow-ups

- PO show demand breakdown uses existing `PurchaseOrderLineDemandBreakdown`; "store replenishment" label retained on breakdown partial.
- Phase 10-E app-wide consistency sweep remains paused.

---

## Next

- Merge branch; tag or note baseline after merge review.
- Resume Phase 10-E or next v0.04 milestone per roadmap.
