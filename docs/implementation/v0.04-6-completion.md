# frozen_string_literal: true

# v0.04-6 Demand Foundation — completion record

## Status

**Complete** — merged on branch `v0.04-6-demand-foundation`.

## Deliverables

- Migration: `demand_line_sequences`, `demand_lines`, `stock_considerations`
- Models: `DemandLine`, `DemandLineSequence`, `StockConsideration`
- Services: `DemandLines::*`, `StockConsiderations::*`
- Permissions: `demand.*`, `stock_considerations.*` via `Seeds::V0046Permissions`
- Workspace: `/demand` (index, show, new, cancel, expire, match variant)
- Stock considerations: create, convert, dismiss
- Item drawer cutover: `Items::DemandActionsController` → `DemandLines::StartFromItem` (no legacy writes)
- Customer workspace: demand lines section on customer show
- Verify: `shelfstack:v0046_verify_demand_foundation` (alias `shelfstack:v0046:verify_demand_foundation`)

## Runtime cutover

New staff demand from item drawer and `/demand` writes **`demand_lines` only**. Legacy `CustomerRequests::StartFromItem` is not invoked from drawer or new demand UI.

Hold actions record **intent only** — no inventory reservation until v0.04-7.

## Verification

```bash
bin/rails db:migrate
bin/rails test test/services/demand_lines test/integration/items_customer_demand_drawer_integration_test.rb
STRICT=1 bin/rails shelfstack:v0046:verify_demand_foundation
```

## Deferred

- `demand_allocations` — v0.04-7
- Nightly expiry job — v0.04-7
- Legacy route removal — v0.04-10

## Next milestone

**v0.04-7 — Allocations and reservations**
