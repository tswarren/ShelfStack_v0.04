<section class="report-section ss-pos-register-summary__box">
  <h2 class="ss-pos-register-summary__box-title">Transaction Mix</h2>
  <table class="ss-pos-register-summary__kv">
    <tbody>
      <% rows.each do |row| %>
        <tr class="<%= "is-total" if row.label == "Units sold" %>">
          <th scope="row"><%= row.label %></th>
          <td><%= pos_report_mix_value(row) %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
</section>
