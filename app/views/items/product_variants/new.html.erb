<%= render "shared/forms/page_header",
           eyebrow: "Items",
           title: "New Product Variant",
           description: "Create a sellable SKU with condition, attributes, and display settings." %>

<%= form_with model: [:items, @product_variant], class: "ss-form ss-form--wide" do |f| %>
  <%= render "form", f: f, product_variant: @product_variant, products: @products, categories: @categories, conditions: @conditions, display_locations: @display_locations %>
  <footer class="ss-form-actions">
    <%= f.submit "Create Variant", class: "ss-btn" %>
    <%= link_to "Cancel", items_product_variants_path, class: "ss-btn ss-btn-secondary" %>
  </footer>
<% end %>
