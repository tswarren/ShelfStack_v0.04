<%= render "shared/forms/page_header",
           eyebrow: "Items",
           title: "Edit Product",
           description: "Update core product details, SKU configuration, and pricing defaults." %>

<%= form_with model: [:items, @product], class: "ss-form ss-form--wide", html: { multipart: true } do |f| %>
  <%= render "form", f: f, product: @product, catalog_items: @catalog_items, display_locations: @display_locations %>
  <footer class="ss-form-actions">
    <%= f.submit "Update Product", class: "ss-btn" %>
    <%= link_to "Cancel", items_product_path(@product), class: "ss-btn ss-btn-secondary" %>
  </footer>
<% end %>
