<%= form_with model: product, url: items_add_item_path(step: "selling_setup"), method: :post, class: "ss-form ss-form--wide", html: { multipart: true } do |f| %>
  <%= render "shared/forms/errors", record: product %>
  <%= render "items/shared/product_forms/catalog_linked",
             f: f,
             product: product,
             sub_departments: sub_departments,
             display_locations: display_locations %>
  <footer class="ss-form-actions">
    <%= render "shared/ui/button", label: "Continue to Sellable SKU", type: :submit, variant: :primary %>
    <%= render "shared/ui/button", label: "Cancel", variant: :tertiary, url: items_item_path(product_id: product.id) %>
  </footer>
<% end %>

<p><%= render "shared/ui/button", label: "Back", variant: :link, url: items_add_item_path(step: "item_details") %></p>
