<%= render "shared/forms/page_header",
           eyebrow: "Customers",
           title: "New Customer",
           description: "Create a customer profile for requests, holds, and pickup." %>

<%= form_with model: @customer, url: customers_customers_path, class: "ss-form ss-form--standard" do |f| %>
  <%= hidden_field_tag :return_to, @return_to if @return_to.present? %>
  <%= render "form", f: f, customer: @customer %>
  <footer class="ss-form-actions">
    <%= render "shared/ui/button", label: "Create Customer", type: :submit, variant: :primary %>
    <%= render "shared/ui/button", label: "Cancel", variant: :tertiary, url: @return_to.presence || customers_customers_path %>
  </footer>
<% end %>
