<%= render "setup/shared/back_link", path: setup_product_variant_vendors_path, label: "Variant Vendors" %>

<%
  header_actions = render(
    "setup/shared/detail_lifecycle_header_actions",
    record: @product_variant_vendor,
    edit_path: edit_setup_product_variant_vendor_path(@product_variant_vendor),
    inactivate_path: inactivate_setup_product_variant_vendor_path(@product_variant_vendor),
    reactivate_path: reactivate_setup_product_variant_vendor_path(@product_variant_vendor)
  )
%>

<%= render "shared/ui/page_header",
           title: "#{@product_variant_vendor.product_variant.sku} — #{@product_variant_vendor.vendor.name}",
           actions: header_actions %>

<dl class="ss-summary">
  <dt>Vendor item number</dt><dd><%= @product_variant_vendor.vendor_item_number || "—" %></dd>
  <dt>Supplier discount</dt><dd><%= format_basis_points(@product_variant_vendor.supplier_discount_bps) %></dd>
  <dt>Returnability</dt><dd><%= @product_variant_vendor.returnability_status ? humanize_controlled_value(@product_variant_vendor.returnability_status) : "—" %></dd>
  <dt>Preferred</dt><dd><%= @product_variant_vendor.preferred? ? "Yes" : "No" %></dd>
  <dt>Status</dt><dd><%= ss_status_badge(status_label(@product_variant_vendor.active?), status: @product_variant_vendor.active? ? :active : :inactive) %></dd>
</dl>

<h2>Audit Timeline</h2>
<%= render "shared/audit_timeline", audit_events: @audit_events %>

<%= render "setup/shared/detail_danger_zone",
           heading_id: "product-variant-vendor-danger-zone-heading",
           message: "Delete removes the variant vendor link only when nothing references it. Prefer inactivating.",
           delete_label: "Delete variant vendor",
           delete_path: setup_product_variant_vendor_path(@product_variant_vendor),
           confirm_message: "Delete this variant vendor?" %>
