<h1>Edit Category</h1>
<%= form_with model: @category, url: setup_category_path(@category), class: "ss-form" do |f| %>
  <%= render "form", f: f, category: @category, departments: @departments, tax_categories: @tax_categories %>
  <%= f.submit "Update Category", class: "ss-btn" %>
<% end %>
