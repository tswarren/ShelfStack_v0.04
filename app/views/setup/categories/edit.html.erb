<%= render "shared/forms/page_header",
           eyebrow: "Setup",
           title: "Edit Category",
           description: "Categories classify sellable SKUs and provide defaults for pricing and tax behavior." %>

<%= form_with model: @category, url: setup_category_path(@category), class: "ss-form ss-form--standard" do |f| %>
  <%= render "form", f: f, category: @category, departments: @departments, tax_categories: @tax_categories %>
  <footer class="ss-form-actions">
    <%= f.submit "Update Category", class: "ss-btn" %>
    <%= link_to "Cancel", setup_category_path(@category), class: "ss-btn ss-btn-secondary" %>
  </footer>
<% end %>
