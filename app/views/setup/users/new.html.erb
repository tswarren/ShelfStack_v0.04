<h1>New User</h1>
<%= form_with model: @user, url: setup_users_path, class: "ss-form" do |f| %>
  <%= render "form", f: f, user: @user, stores: @stores %>
  <%= f.submit "Create User", class: "ss-btn" %>
<% end %>
