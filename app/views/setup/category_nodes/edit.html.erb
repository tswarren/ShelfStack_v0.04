<%= render "shared/forms/page_header",
           eyebrow: "Setup",
           title: "Edit Category Node",
           description: "Update #{@category_node.name} in #{@category_scheme.name}." %>

<%= render "form" %>
